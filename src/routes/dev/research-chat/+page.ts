import type { PageLoad } from "./$types";

export const load: PageLoad = () => ({
  seo: {
    title: "Research chat — Lightkeepers dev",
    description: "Prototype TC research chat against lightkeepers-agent.",
  },
});
