import type { PageLoad } from "./$types";

export const load: PageLoad = () => ({
  seo: {
    title: "Stygian Fast Clears Demo — Lightkeepers",
    description:
      "Dev demo: fastest Fearless clears under a cost cap for your roster.",
  },
});
