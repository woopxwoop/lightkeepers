import type { PageLoad } from "./$types";

export const load: PageLoad = () => ({
  seo: {
    title: "UI gallery — Lightkeepers",
    description: "Internal component and token gallery for design system work.",
  },
});
